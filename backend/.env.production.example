NODE_ENV=production
PORT=5000

# MongoDB Atlas Connection
# IMPORTANT: Replace with your actual MongoDB Atlas connection string
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/physicsium?retryWrites=true&w=majority

# JWT Secret - CRITICAL: Use a strong random string (minimum 32 characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_production_jwt_secret_minimum_32_characters_long_random_string

# Frontend URL for CORS - Your Vercel deployment URL
# Multiple URLs can be comma-separated: https://app1.vercel.app,https://app2.vercel.app
CLIENT_URL=https://your-app.vercel.app

# Cloudinary Configuration (for image uploads)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# SECURITY NOTES:
# 1. Never commit this file to version control
# 2. Keep JWT_SECRET cryptographically random and secure
# 3. Use MongoDB Atlas IP whitelist to restrict database access
# 4. Enable MongoDB Atlas encryption at rest
# 5. Use environment-specific URLs for CLIENT_URL
# 6. Rotate secrets regularly
# 7. Use strong passwords for MongoDB users
